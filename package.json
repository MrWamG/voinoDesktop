{
  "name": "blogs",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron ./main.js",
	"package": "electron-packager . electron-quick-start --overwrite --win=x32 --out ./voinoMusicPlayer --arch=x64 --app-version=1.0.0 --electron-version=2.0.0"
  },
  "keywords": [],
  "author": "Silenzio",
  "license": "ISC",
  "devDependencies": {
    "electron": "^6.0.10"
  }
}